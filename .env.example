# Create a local `.env` or export these values in your shell. Never commit a real key.
API_FOOTBALL_KEY=replace_with_your_api_football_key

# Keep 20 of the free plan's 100 daily requests in reserve.
API_FOOTBALL_DAILY_BUDGET=80
